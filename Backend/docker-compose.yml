services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # rabbitmq intentionally omitted until events are wired (T04/T26) — add
  # a "rabbitmq: image: rabbitmq:3-management" service here when needed.

volumes:
  redis_data:
